# Core crawling - Crawl4AI with AdaptiveCrawler support
crawl4ai>=0.7.0

# Web framework
fastapi
uvicorn[standard]

# Async HTTP client for API calls (DeepSeek, OpenRouter)
httpx

# For cosine similarity calculations (embeddings)
numpy

# Local embedding model for native embedding strategy during crawling
# Uses paraphrase-multilingual-MiniLM-L12-v2 for Chinese/multilingual support
sentence-transformers
